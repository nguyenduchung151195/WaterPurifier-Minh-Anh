/*
 * ApproveDialog Messages
 *
 * This contains all the text for the ApproveDialog component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.ApproveDialog';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the ApproveDialog component!',
  },
});
