/*
 * AsyncComponent Messages
 *
 * This contains all the text for the AsyncComponent component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.AsyncComponent';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the AsyncComponent component!',
  },
});
