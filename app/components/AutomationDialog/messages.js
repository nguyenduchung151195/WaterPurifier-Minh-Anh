/*
 * AutomationDialog Messages
 *
 * This contains all the text for the AutomationDialog component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.AutomationDialog';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the AutomationDialog component!',
  },
});
