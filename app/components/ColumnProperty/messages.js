/*
 * ColumnProperty Messages
 *
 * This contains all the text for the ColumnProperty component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.ColumnProperty';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the ColumnProperty component!',
  },
});
