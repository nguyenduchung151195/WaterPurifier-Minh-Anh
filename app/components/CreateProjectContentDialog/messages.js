/*
 * CreateProjectContentDialog Messages
 *
 * This contains all the text for the CreateProjectContentDialog component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.CreateProjectContentDialog';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the CreateProjectContentDialog component!',
  },
});
