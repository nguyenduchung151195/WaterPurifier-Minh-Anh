/*
 * DialogFile Messages
 *
 * This contains all the text for the DialogFile component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.DialogFile';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the DialogFile component!',
  },
});
