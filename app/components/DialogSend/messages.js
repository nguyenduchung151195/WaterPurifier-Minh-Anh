/*
 * DialogSend Messages
 *
 * This contains all the text for the DialogSend component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.DialogSend';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the DialogSend component!',
  },
});
