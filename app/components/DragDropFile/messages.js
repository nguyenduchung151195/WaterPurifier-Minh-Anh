/*
 * DragDropFile Messages
 *
 * This contains all the text for the DragDropFile component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.DragDropFile';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the DragDropFile component!',
  },
});
