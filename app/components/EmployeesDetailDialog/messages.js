/*
 * EmployeesDetailDialog Messages
 *
 * This contains all the text for the EmployeesDetailDialog component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.EmployeesDetailDialog';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the EmployeesDetailDialog component!',
  },
});
