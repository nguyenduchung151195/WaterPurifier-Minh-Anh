/*
 * ExportStockDialog Messages
 *
 * This contains all the text for the ExportStockDialog component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.ExportStockDialog';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the ExportStockDialog component!',
  },
});
