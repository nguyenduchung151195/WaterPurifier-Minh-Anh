/*
 * ImportFile Messages
 *
 * This contains all the text for the ImportFile component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.ImportFile';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the ImportFile component!',
  },
});
