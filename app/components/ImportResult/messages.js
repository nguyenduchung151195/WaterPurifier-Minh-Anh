/*
 * ImportResult Messages
 *
 * This contains all the text for the ImportResult component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.ImportResult';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the ImportResult component!',
  },
});
