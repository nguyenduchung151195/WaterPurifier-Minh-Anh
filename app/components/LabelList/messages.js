/*
 * LabelList Messages
 *
 * This contains all the text for the LabelList component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.LabelList';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the LabelList component!',
  },
});
