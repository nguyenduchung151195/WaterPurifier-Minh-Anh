/*
 * MeetingDialogContent Messages
 *
 * This contains all the text for the MeetingDialogContent component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.MeetingDialogContent';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the MeetingDialogContent component!',
  },
});
