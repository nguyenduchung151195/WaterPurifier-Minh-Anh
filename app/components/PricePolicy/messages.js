/*
 * PricePolicy Messages
 *
 * This contains all the text for the PricePolicy component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.PricePolicy';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the PricePolicy component!',
  },
});
