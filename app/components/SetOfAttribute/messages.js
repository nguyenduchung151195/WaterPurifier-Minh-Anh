/*
 * SetOfAttribute Messages
 *
 * This contains all the text for the SetOfAttribute component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.SetOfAttribute';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the SetOfAttribute component!',
  },
});
