/*
 * TableBodyImport Messages
 *
 * This contains all the text for the TableBodyImport component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.TableBodyImport';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the TableBodyImport component!',
  },
});
