/*
 * TaskProperty Messages
 *
 * This contains all the text for the TaskProperty component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.TaskProperty';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the TaskProperty component!',
  },
});
