/*
 * TextFieldCode Messages
 *
 * This contains all the text for the TextFieldCode component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.TextFieldCode';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the TextFieldCode component!',
  },
});
