/*
 * TimelineEvent Messages
 *
 * This contains all the text for the TimelineEvent component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.TimelineEvent';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the TimelineEvent component!',
  },
});
