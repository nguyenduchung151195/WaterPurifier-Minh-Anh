/*
 * UnitStock Messages
 *
 * This contains all the text for the UnitStock component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.UnitStock';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the UnitStock component!',
  },
});
