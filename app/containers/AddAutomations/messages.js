/*
 * AddAdvancePage Messages
 *
 * This contains all the text for the AddAdvancePage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.AddAutomations';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the AddAutomations container!',
  },
});
