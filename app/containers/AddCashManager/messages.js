/*
 * AddCashManager Messages
 *
 * This contains all the text for the AddCashManager container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.AddCashManager';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the AddCashManager container!',
  },
});
