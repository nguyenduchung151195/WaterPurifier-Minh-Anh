/*
 * AddDelivery Messages
 *
 * This contains all the text for the AddDelivery container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.AddDelivery';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the AddDelivery container!',
  },
});
