/*
 * AddDeliveryPage Messages
 *
 * This contains all the text for the AddDeliveryPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.AddDeliveryPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the AddDeliveryPage container!',
  },
});
