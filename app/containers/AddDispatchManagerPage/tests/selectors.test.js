// import { fromJS } from 'immutable';
// import { selectAddDispatchManagerPageDomain } from '../selectors';

describe('selectAddDispatchManagerPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
