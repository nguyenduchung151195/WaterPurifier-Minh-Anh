/*
 * AddEmailHistory Messages
 *
 * This contains all the text for the AddEmailHistory container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.AddEmailHistory';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the AddEmailHistory container!',
  },
});
