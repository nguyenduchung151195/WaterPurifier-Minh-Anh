/*
 * AddGeneralManager Messages
 *
 * This contains all the text for the AddGeneralManager container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.AddGeneralManager';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the AddGeneralManager container!',
  },
});
