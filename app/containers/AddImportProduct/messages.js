/*
 * AddImportProduct Messages
 *
 * This contains all the text for the AddImportProduct container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.AddImportProduct';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the AddImportProduct container!',
  },
});
