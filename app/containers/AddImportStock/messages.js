/*
 * AddImportProduct Messages
 *
 * This contains all the text for the AddImportProduct container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.AddImportStock';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the AddImportStock container!',
  },
});
