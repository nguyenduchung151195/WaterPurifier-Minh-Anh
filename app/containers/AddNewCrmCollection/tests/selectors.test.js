// import { fromJS } from 'immutable';
// import { selectAddNewCrmCollectionDomain } from '../selectors';

describe('selectAddNewCrmCollectionDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
