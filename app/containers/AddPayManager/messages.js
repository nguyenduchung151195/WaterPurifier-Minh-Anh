/*
 * AddPayManager Messages
 *
 * This contains all the text for the AddPayManager container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.AddPayManager';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the AddPayManager container!',
  },
});
