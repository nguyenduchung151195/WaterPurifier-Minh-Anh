/*
 * AddPaymentPage Messages
 *
 * This contains all the text for the AddPaymentPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.AddPaymentPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the AddPaymentPage container!',
  },
});
