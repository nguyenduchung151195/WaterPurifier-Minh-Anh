/*
 * AddPersonnel Messages
 *
 * This contains all the text for the AddPersonnel container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.AddPersonnel';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the AddPersonnel container!',
  },
});
