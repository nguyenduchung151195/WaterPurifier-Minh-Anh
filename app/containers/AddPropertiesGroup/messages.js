/*
 * AddPropertiesGroup Messages
 *
 * This contains all the text for the AddPropertiesGroup container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.AddPropertiesGroup';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the AddPropertiesGroup container!',
  },
});
