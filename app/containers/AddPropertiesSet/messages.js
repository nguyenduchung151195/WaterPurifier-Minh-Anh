/*
 * AddPropertiesSet Messages
 *
 * This contains all the text for the AddPropertiesSet container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.AddPropertiesSet';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the AddPropertiesSet container!',
  },
});
