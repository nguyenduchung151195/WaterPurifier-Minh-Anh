/*
 * AddReceivableManager Messages
 *
 * This contains all the text for the AddReceivableManager container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.AddReceivableManager';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the AddReceivableManager container!',
  },
});
