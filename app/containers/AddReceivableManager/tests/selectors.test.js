// import { fromJS } from 'immutable';
// import { selectAddReceivableManagerDomain } from '../selectors';

describe('selectAddReceivableManagerDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
