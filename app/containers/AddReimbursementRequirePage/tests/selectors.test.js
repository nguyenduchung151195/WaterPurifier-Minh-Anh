// import { fromJS } from 'immutable';
// import { selectAddReimbursementRequirePageDomain } from '../selectors';

describe('selectAddReimbursementRequirePageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
