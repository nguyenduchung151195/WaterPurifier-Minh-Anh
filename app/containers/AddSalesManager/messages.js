/*
 * AddSalesManager Messages
 *
 * This contains all the text for the AddSalesManager container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.AddSalesManager';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the AddSalesManager container!',
  },
});
