/*
 * AddSalesPolicy Messages
 *
 * This contains all the text for the AddSalesPolicy container.
 */

import { defineMessages } from 'react-intl';
import { getDataI18N } from '../../utils/common';
const data = require('../../translations/en/AddSalesPolicy.json');
export const scope = 'crm.AddSalesPolicy';

export default defineMessages(getDataI18N(scope, data));
