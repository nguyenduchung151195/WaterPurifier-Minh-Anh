/*
 * AddSampleProcess Messages
 *
 * This contains all the text for the AddSampleProcess container.
 */

import { defineMessages } from 'react-intl';
import { getDataI18N } from '../../utils/common';
const data = require('../../translations/en/AddSampleProcess.json');
export const scope = 'task.AddSampleProcess';

export default defineMessages(getDataI18N(scope, data));
