/*
 * AddSmsCampaign Messages
 *
 * This contains all the text for the AddSmsCampaign container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.AddSmsCampaign';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the AddSmsCampaign container!',
  },
});
