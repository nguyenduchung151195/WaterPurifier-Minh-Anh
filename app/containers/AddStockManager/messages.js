/*
 * AddStockManager Messages
 *
 * This contains all the text for the AddStockManager container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.AddStockManager';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the AddStockManager container!',
  },
});
