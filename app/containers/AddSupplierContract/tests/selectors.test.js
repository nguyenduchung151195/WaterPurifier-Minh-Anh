// import { fromJS } from 'immutable';
// import { selectAddSupplierContractDomain } from '../selectors';

describe('selectAddSupplierContractDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
