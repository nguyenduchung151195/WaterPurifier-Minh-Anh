// import { fromJS } from 'immutable';
// import { selectApproveGroupDetailPageDomain } from '../selectors';

describe('selectApproveGroupDetailPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
