/*
 * ApproveGroupPage Messages
 *
 * This contains all the text for the ApproveGroupPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.ApproveGroupPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the ApproveGroupPage container!',
  },
});
