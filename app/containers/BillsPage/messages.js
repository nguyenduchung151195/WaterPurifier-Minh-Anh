/*
 * BillsPage Messages
 *
 * This contains all the text for the BillsPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.BillsPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the BillsPage container!',
  },
});
