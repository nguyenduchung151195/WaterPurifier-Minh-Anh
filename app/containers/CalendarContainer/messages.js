/*
 * CalendarContainer Messages
 *
 * This contains all the text for the CalendarContainer container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.CalendarContainer';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the CalendarContainer container!',
  },
});
