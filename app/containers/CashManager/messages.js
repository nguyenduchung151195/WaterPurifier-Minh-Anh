/*
 * CashManager Messages
 *
 * This contains all the text for the CashManager container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.CashManager';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the CashManager container!',
  },
});
