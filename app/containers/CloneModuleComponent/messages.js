/*
 * CloneModuleComponent Messages
 *
 * This contains all the text for the CloneModuleComponent container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.CloneModuleComponent';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the CloneModuleComponent container!',
  },
});
