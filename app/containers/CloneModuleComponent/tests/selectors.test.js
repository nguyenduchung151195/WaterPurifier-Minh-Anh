// import { fromJS } from 'immutable';
// import { selectCloneModuleComponentDomain } from '../selectors';

describe('selectCloneModuleComponentDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
