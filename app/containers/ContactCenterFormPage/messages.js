/*
 * ContactCenterFormPage Messages
 *
 * This contains all the text for the ContactCenterFormPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.ContactCenterFormPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the ContactCenterFormPage container!',
  },
});
