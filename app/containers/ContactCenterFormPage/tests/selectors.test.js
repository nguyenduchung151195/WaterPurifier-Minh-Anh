// import { fromJS } from 'immutable';
// import { selectContactCenterFormPageDomain } from '../selectors';

describe('selectContactCenterFormPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
