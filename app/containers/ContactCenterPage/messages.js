/*
 * ContactCenterPage Messages
 *
 * This contains all the text for the ContactCenterPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.ContactCenterPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the ContactCenterPage container!',
  },
});
