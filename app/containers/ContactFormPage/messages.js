/*
 * ContactFormPage Messages
 *
 * This contains all the text for the ContactFormPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.ContactFormPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the ContactFormPage container!',
  },
});
