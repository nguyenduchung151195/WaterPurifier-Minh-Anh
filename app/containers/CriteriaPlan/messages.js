/*
 * CriteriaPlan Messages
 *
 * This contains all the text for the CriteriaPlan container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.CriteriaPlan';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the CriteriaPlan container!',
  },
});
