/*
 * DeliveryPage Messages
 *
 * This contains all the text for the DeliveryPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.DeliveryPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the DeliveryPage container!',
  },
});
