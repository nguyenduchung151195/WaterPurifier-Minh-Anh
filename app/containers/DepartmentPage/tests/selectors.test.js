// import { fromJS } from 'immutable';
// import { selectListOfDepartmentPageDomain } from '../selectors';

describe('selectListOfDepartmentPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
