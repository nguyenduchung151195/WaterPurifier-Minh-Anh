/*
 * DispatchManager Messages
 *
 * This contains all the text for the DispatchManager container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.DispatchManager';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the DispatchManager container!',
  },
});
