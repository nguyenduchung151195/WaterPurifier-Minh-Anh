/*
 * DispatchManagerGo Messages
 *
 * This contains all the text for the DispatchManagerGo container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.DispatchManagerGo';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the DispatchManagerGo container!',
  },
});
