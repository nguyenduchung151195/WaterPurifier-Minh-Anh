/*
 * EditContractPage Messages
 *
 * This contains all the text for the EditContractPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.EditContractPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the EditContractPage container!',
  },
});
