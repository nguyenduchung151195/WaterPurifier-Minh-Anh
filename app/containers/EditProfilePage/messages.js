/*
 * EditProfilePage Messages
 *
 * This contains all the text for the EditProfilePage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.EditProfilePage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the EditProfilePage container!',
  },
});
