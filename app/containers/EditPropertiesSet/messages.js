/*
 * EditPropertiesSet Messages
 *
 * This contains all the text for the EditPropertiesSet container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.EditPropertiesSet';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the EditPropertiesSet container!',
  },
});
