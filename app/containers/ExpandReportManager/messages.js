/*
 * ExpandReportManager Messages
 *
 * This contains all the text for the ExpandReportManager container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.ExpandReportManager';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the ExpandReportManager container!',
  },
});
