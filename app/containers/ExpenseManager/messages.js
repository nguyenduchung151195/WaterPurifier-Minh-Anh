/*
 * ExpenseManager Messages
 *
 * This contains all the text for the ExpenseManager container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.ExpenseManager';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the ExpenseManager container!',
  },
});
