/*
 * FavoritePage Messages
 *
 * This contains all the text for the FavoritePage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.FavoritePage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the FavoritePage container!',
  },
});
