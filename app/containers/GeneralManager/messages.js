/*
 * GeneralManager Messages
 *
 * This contains all the text for the GeneralManager container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.GeneralManager';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the GeneralManager container!',
  },
});
