/*
 * EditEmployees Messages
 *
 * This contains all the text for the EditEmployees container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.EditEmployees';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the EditEmployees container!',
  },
});
