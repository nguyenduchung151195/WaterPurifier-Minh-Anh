/*
 * SalaryPage Messages
 *
 * This contains all the text for the SalaryPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.SalaryPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the SalaryPage container!',
  },
});
