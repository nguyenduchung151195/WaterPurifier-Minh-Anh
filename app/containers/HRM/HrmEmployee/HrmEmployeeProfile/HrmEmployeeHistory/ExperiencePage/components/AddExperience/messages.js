/*
 * AddExperience Messages
 *
 * This contains all the text for the AddExperience container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.AddExperience';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the AddExperience container!',
  },
});
