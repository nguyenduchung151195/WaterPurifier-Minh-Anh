/*
 * ExperiencePage Messages
 *
 * This contains all the text for the ExperiencePage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.ExperiencePage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the ExperiencePage container!',
  },
});
