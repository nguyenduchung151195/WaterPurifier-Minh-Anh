/*
 * AddProcess Messages
 *
 * This contains all the text for the AddProcess container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.AddProcess';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the AddProcess container!',
  },
});
