/*
 * AddRelation Messages
 *
 * This contains all the text for the AddRelation container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.AddRelation';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the AddRelation container!',
  },
});
