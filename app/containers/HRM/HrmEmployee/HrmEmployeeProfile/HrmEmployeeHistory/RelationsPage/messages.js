/*
 * RelationPage Messages
 *
 * This contains all the text for the RelationPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.RelationPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the RelationPage container!',
  },
});
