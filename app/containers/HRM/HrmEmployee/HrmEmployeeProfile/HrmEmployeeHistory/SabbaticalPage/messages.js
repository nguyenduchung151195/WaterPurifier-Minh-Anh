/*
 * SabbaticalPage Messages
 *
 * This contains all the text for the SabbaticalPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.SabbaticalPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the SabbaticalPage container!',
  },
});
