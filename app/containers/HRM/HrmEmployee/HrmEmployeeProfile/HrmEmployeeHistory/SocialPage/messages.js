/*
 * SocialPage Messages
 *
 * This contains all the text for the SocialPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.SocialPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the SocialPage container!',
  },
});
