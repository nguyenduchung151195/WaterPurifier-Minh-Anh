/*
 * AddCollaborate Messages
 *
 * This contains all the text for the AddCollaborate container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.AddCollaborate';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the AddCollaborate container!',
  },
});
