/*
 * CollaboratePage Messages
 *
 * This contains all the text for the CollaboratePage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.CollaboratePage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the CollaboratePage container!',
  },
});
