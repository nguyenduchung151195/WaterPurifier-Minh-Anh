/*
 * AddDismissed Messages
 *
 * This contains all the text for the AddDismissed container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.AddDismissed';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the AddDismissed container!',
  },
});
