/*
 * AddPraise Messages
 *
 * This contains all the text for the AddPraise container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.AddPraise';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the AddPraise container!',
  },
});
