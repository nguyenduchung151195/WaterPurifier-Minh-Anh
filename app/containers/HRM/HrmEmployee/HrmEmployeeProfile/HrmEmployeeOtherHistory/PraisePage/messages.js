/*
 * PraisePage Messages
 *
 * This contains all the text for the PraisePage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.PraisePage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the PraisePage container!',
  },
});
