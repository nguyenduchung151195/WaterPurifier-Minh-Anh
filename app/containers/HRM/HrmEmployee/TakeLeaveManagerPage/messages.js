/*
 * TakeLeaveManagePage Messages
 *
 * This contains all the text for the TakeLeaveManagePage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.TakeLeaveManagePage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the TakeLeaveManagePage container!',
  },
});
