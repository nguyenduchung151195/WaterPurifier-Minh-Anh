/*
 * AddRecruitmentManagement Messages
 *
 * This contains all the text for the AddRecruitmentManagement container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.AddRecruitmentManagement';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the AddRecruitmentManagement container!',
  },
});
