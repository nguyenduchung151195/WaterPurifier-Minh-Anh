/*
 * AddRecruitment Messages
 *
 * This contains all the text for the AddRecruitment container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.AddRecruitment';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the AddRecruitment container!',
  },
});
