/*
 * RecruitmentManagement Messages
 *
 * This contains all the text for the RecruitmentManagement container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.RecruitmentManagement';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the RecruitmentManagement container!',
  },
});
