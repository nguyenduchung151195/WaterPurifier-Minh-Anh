import { defineMessages } from 'react-intl';

export const scope = 'app.containers.AddSocialInsurance';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the AddSocialInsurance container!',
  },
});
