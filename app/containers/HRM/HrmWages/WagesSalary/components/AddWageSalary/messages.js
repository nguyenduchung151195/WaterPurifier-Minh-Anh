/*
 * AddWages Messages
 *
 * This contains all the text for the AddWages container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.AddWages';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the AddWages container!',
  },
});
