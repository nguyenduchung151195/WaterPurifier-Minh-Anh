/*
 * WageSalary Messages
 *
 * This contains all the text for the WageSalary container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.WageSalary';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the WageSalary container!',
  },
});
