/*
 * HistoryLog Messages
 *
 * This contains all the text for the HistoryLog container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.HistoryLog';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the HistoryLog container!',
  },
});
