/*
 * HocTable Messages
 *
 * This contains all the text for the HocTable container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.HocTable';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the HocTable container!',
  },
});
