/*
 * InventoryDetailPage Messages
 *
 * This contains all the text for the InventoryDetailPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.InventoryDetailPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the InventoryDetailPage container!',
  },
});
