// import { fromJS } from 'immutable';
// import { selectInventoryDetailPageDomain } from '../selectors';

describe('selectInventoryDetailPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
