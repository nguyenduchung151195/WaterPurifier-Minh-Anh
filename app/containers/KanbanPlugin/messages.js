/*
 * KanbanPlugin Messages
 *
 * This contains all the text for the KanbanPlugin container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.KanbanPlugin';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the KanbanPlugin container!',
  },
});
