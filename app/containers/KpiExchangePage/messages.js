/*
 * KpiExchangePage Messages
 *
 * This contains all the text for the KpiExchangePage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.KpiExchangePage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the KpiExchangePage container!',
  },
});
