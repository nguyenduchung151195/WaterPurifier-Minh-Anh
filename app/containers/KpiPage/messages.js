/*
 * KpiPage Messages
 *
 * This contains all the text for the KpiPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.KpiPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the KpiPage container!',
  },
});
