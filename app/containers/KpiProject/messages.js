/*
 * KpiProject Messages
 *
 * This contains all the text for the KpiProject container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.KpiProject';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the KpiProject container!',
  },
});
