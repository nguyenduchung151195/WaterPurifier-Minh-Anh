/*
 * MeetingRoom Messages
 *
 * This contains all the text for the MeetingRoom container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.MeetingRoom';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the MeetingRoom container!',
  },
});
