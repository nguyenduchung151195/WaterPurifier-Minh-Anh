/*
 * PayManager Messages
 *
 * This contains all the text for the PayManager container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.PayManager';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the PayManager container!',
  },
});
