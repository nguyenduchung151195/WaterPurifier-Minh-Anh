/*
 * PersonalPage Messages
 *
 * This contains all the text for the PersonalPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.PersonalPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the PersonalPage container!',
  },
});
