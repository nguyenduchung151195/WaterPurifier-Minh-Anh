/*
 * ReceivableManager Messages
 *
 * This contains all the text for the ReceivableManager container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.ReceivableManager';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the ReceivableManager container!',
  },
});
