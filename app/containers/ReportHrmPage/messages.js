/*
 * ReportHrmPage Messages
 *
 * This contains all the text for the ReportHrmPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.ReportHrmPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the ReportHrmPage container!',
  },
});
