// import { fromJS } from 'immutable';
// import { selectReportReportCustomerDomain } from '../selectors';

describe('selectReportReportCustomerDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
