/*
 * ReportSource Messages
 *
 * This contains all the text for the ReportSource container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.ReportSource';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the ReportSource container!',
  },
});
