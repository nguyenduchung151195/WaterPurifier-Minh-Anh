/*
 * ReportTask Messages
 *
 * This contains all the text for the ReportTask container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.ReportTask';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the ReportTask container!',
  },
});
