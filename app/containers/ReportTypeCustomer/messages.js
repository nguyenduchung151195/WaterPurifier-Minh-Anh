/*
 * ReportTypeCustomer Messages
 *
 * This contains all the text for the ReportTypeCustomer container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.ReportTypeCustomer';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the ReportTypeCustomer container!',
  },
});
