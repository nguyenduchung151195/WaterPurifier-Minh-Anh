/*
 * SalesManager Messages
 *
 * This contains all the text for the SalesManager container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.SalesManager';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the SalesManager container!',
  },
});
