/*
 * SourcePage Messages
 *
 * This contains all the text for the SourcePage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.SourcePage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the SourcePage container!',
  },
});
