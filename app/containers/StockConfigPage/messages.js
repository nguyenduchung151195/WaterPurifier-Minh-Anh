/*
 * StockConfigPage Messages
 *
 * This contains all the text for the StockConfigPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.StockConfigPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the StockConfigPage container!',
  },
});
