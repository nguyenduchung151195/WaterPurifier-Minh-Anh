/*
 * TableContainer Messages
 *
 * This contains all the text for the TableContainer container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.TableContainer';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the TableContainer container!',
  },
});
