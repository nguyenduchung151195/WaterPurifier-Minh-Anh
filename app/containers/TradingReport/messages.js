/*
 * TradingReport Messages
 *
 * This contains all the text for the TradingReport container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.TradingReport';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the TradingReport container!',
  },
});
