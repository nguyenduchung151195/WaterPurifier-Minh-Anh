/*
 * WorkFlow Messages
 *
 * This contains all the text for the WorkFlow container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.WorkFlow';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the WorkFlow container!',
  },
});
